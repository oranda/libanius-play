# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.QuizScreenController.index
GET     /status                     controllers.QuizScreenController.getStatus()
POST    /processUserAnswer          controllers.QuizScreenController.processUserAnswer()
POST    /removeCurrentWord          controllers.QuizScreenController.removeCurrentWord()
GET     /assets/javascripts/routes  controllers.QuizScreenController.javascriptRoutes()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
